interface:
  display_name: "Limux A2A"
  short_description: "Run and control visible Limux terminals"
  default_prompt: "Use $limux-a2a to run or coordinate visible terminal work."

policy:
  allow_implicit_invocation: true
